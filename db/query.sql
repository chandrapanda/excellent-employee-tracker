USE company_db;

-- Select employees.id, employees.first_name, employees.last_name, roles.job_title, departments.department_name, roles.salary
-- FROM employees
-- JOIN role
-- ON employees.role_id = roles.id
-- JOIN departments
-- ON departments.id = roles.departments.id
-- JOIN employees manager_id;
